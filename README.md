# SnowLib

> **SnowLib**는 Bukkit / Spigot / Paper 환경에서\
> **고급 플러그인 개발자**를 위해 설계된 **모듈형 라이브러리
> 프레임워크**입니다.
>
> 커맨드, 이벤트, GUI, 아이템, 레지스트리, 태스크, 텍스트 시스템부터\
> Attribute / Enchantment 확장까지 포함하는\
> **NMS 친화적 · 구조 중심 · 확장 지향 프레임워크**입니다.

------------------------------------------------------------------------

## 설계 목표

-   반복적인 Bukkit 보일러플레이트 코드 제거
-   Minecraft 내부 설계(Registry, ResourceKey, Context) 존중
-   단순 유틸이 아닌 **프레임워크 레벨 API** 제공
-   Java / Kotlin 완전 대응
-   대규모 플러그인 및 공용 API 제작에 적합

------------------------------------------------------------------------

## 모듈 구성

    snowlib
    ├─ snowlib-core
    │  └─ Java 기반 핵심 프레임워크
    └─ snowlib-kotlin
       └─ Kotlin DSL 및 확장 API

------------------------------------------------------------------------

## 설치 방법

### Gradle (Kotlin DSL)

````yml
depend: [ SnowLib ]
````

``` kotlin

repositories {
    mavenCentral()
}

dependencies {
    compileOnly("io.github.snow1026:snowlib:<version>")
}
```

------------------------------------------------------------------------

## 1️⃣ 커맨드 프레임워크 (Sommand)

Brigadier 스타일의 트리 기반 커맨드 시스템

``` java
Sommand.create("test")
    .executes(ctx -> {
        ctx.sender().sendMessage("Hello SnowLib");
    })
    .register();
```

------------------------------------------------------------------------

## 2️⃣ 이벤트 시스템 (SnowEvent)

``` java
Events.listen(PlayerJoinEvent.class, event -> {
    e.getPlayer().sendMessage("관리자 입장");
    })
    .filter(e -> e.getPlayer().isOp())
```

------------------------------------------------------------------------

## 3️⃣ GUI 프레임워크

``` java
GUI gui = GUI.create(3, "메뉴");
gui.open(player);
```

------------------------------------------------------------------------

## 4️⃣ 아이템 시스템

``` java
ItemStack item = ItemBuilder.of(Material.DIAMOND_SWORD)
    .name("§bSnow Sword")
    .lore("강력한 검")
    .build();
```

------------------------------------------------------------------------

## 5️⃣ Attribute / Enchantment 확장

-   Registry 기반 NMS 안전 확장
-   ResourceKey / SnowKey 사용

------------------------------------------------------------------------

## 6️⃣ Registry 시스템

SnowLib 전반을 관통하는 핵심 인프라

------------------------------------------------------------------------

## 7️⃣ 태스크 시스템

``` java
Tasker.sync()
    .delay(20)
    .repeat(20)
    .run(() -> Bukkit.broadcastMessage("Tick"));
```

------------------------------------------------------------------------

## 8️⃣ Kotlin DSL

``` kotlin
sommand("test") {
    executes {
        sender.sendMessage("Hello Kotlin")
    }
}
```

------------------------------------------------------------------------

## 📜 라이선스

LICENSE 파일을 참조하세요.

------------------------------------------------------------------------

Made by snow
